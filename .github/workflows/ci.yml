name: ci

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
      - name: lint
        run: uvx ruff check src tests
      - name: tests
        run: uv run --group dev pytest -q
      - name: the site must not serve a cached copy of a changed file
        # A stale version stamp keeps the old script live while the repository
        # holds the new one, and nothing about the source explains it.
        run: uv run python scripts/stamp_assets.py --check

      - name: the site must agree with the pipeline
        # A stale bundle would let the page publish a number the estimator no
        # longer produces. Regenerate it and fail if the committed copy differs.
        run: |
          uv run tokidx export-web --out web/data
          git diff --exit-code -- web/data
